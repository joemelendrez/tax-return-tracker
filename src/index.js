import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import TaxReturnTracker from './TaxReturnTracker';

ReactDOM.render(
  <React.StrictMode>
    <TaxReturnTracker />
  </React.StrictMode>,
  document.getElementById('root')
);